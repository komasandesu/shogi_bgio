export type MovablePlaceType = null | Array<[number,number]> // [place1,place2,...]

const empty = null
const a:Array<[number,number]> = [[1,2]]

const MovablePlace = {
    empty,
    a
}
export default MovablePlace